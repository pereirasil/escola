-- Adiciona coluna monthly_fee na tabela students (valor da mensalidade por aluno)
-- Executar no MySQL: ALTER TABLE students ADD COLUMN monthly_fee DECIMAL(10,2) NULL;

ALTER TABLE students ADD COLUMN monthly_fee DECIMAL(10,2) NULL;
